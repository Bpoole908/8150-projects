problem:
  region: us # optionsL: us or au
  domain_size: 4 # number of colors to work with
csp:              
  seed: Null
  forward_checking: True # enable forward checking
  propagation: True # enable propagation through singletons 
  heuristics: True  # enable heuristics for unassigned selection and domain selection